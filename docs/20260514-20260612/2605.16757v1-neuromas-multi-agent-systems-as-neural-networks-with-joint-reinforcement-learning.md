---
title: "NeuroMAS: Multi-Agent Systems as Neural Networks with Joint Reinforcement Learning"
title_zh: NeuroMAS：基于联合强化学习的神经网络化多智能体系统
authors: "Haoran Lu, Luyang Fang, Wenxuan Zhong, Ping Ma"
date: 2026-05-16
pdf: "https://arxiv.org/pdf/2605.16757v1"
tags: ["query:agent-build"]
score: 9.0
evidence: 类神经网络的多智能体架构
tldr: 传统多智能体语言系统依赖手工设计工作流，限制了可扩展性。NeuroMAS将多智能体系统重构为类神经网络架构，通过强化学习训练节点间的通信与协作，无需预定义角色。实验证明该方法在性能上显著超越现有基线，且系统规模可通过渐进式增长有效扩展，为LLM提供新的缩放维度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.16757v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1415, \"height\": 537, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.16757v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 712, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16757v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 915, \"height\": 658, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16757v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 831, \"height\": 224, \"label\": \"Table\"}]"
motivation: 克服手工设计工作流在可扩展性上的局限，实现多智能体系统的自动化设计。
method: 将LLM代理视为神经网络节点，通过强化学习联合训练拓扑结构下的通信与专业化。
result: 在多项任务上显著超越推理时和训练的多智能体基线；发现渐进式增长优于从头训练大型系统。
conclusion: 神经多智能体系统可作为LLM有前景的缩放策略，通过架构设计替代繁琐的工作流工程。
---

## 摘要
多智能体语言系统通常被构建为手工设计的工作流，其中智能体被分配语义角色，通信协议被预先指定。我们提出NeuroMAS，该方法首先将多智能体语言系统视为一种可训练且可扩展的类神经网络架构，其中LLM智能体作为节点，中间文本信号作为边。在NeuroMAS中，智能体节点无角色但具有结构感知能力：拓扑结构仅决定信息的一般流动方式，而强化学习训练则决定节点如何通信、专业化及协调。这种形式将多智能体设计从工作流工程转向架构设计，其中深度、宽度、连接性和增长协议成为可扩展的能力来源。此外，我们提供了一个理论视角，表明当任务允许层次化分解时，这种模块化文本计算在参数效率上更具优势。实验显示，NeuroMAS在推理时和训练后的多智能体基线上均有显著提升。我们进一步发现，组织扩展具有路径依赖性：从零开始训练较大系统可能具有挑战性，但从较小的训练系统逐步增长则变得可行。这些结果表明，学习型神经多智能体系统是大语言模型有前途的扩展维度。

## Abstract
Multi-agent language systems are often built as hand-designed workflows, where agents are assigned semantic roles and communication protocols are specified in advance. We propose NeuroMAS, a method that first treats a multi-agent language system as a trainable and scalable neural-network-like architecture with LLM agents as nodes and intermediate textual signals as edges. In NeuroMAS, agent nodes are role-free but structure-aware: the topology only determines how information can flow in general, while reinforcement learning training determines how nodes communicate, specialize, and coordinate. This formulation shifts multi-agent design from workflow engineering toward architecture design, where depth, width, connectivity, and growth protocol become scalable sources of capability. Further, we provide a theoretical perspective showing why such modular textual computation is more parameter-efficient when tasks admit hierarchical decompositions. Experiments show that NeuroMAS improves significantly over both inference-time and trained multi-agent baselines. We further find that organizational scaling is path-dependent: larger systems can be challenging to train from scratch, but become feasible when grown progressively from smaller trained systems. These results suggest that learned neural multi-agent systems are a promising scaling axis for LLMs.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前大语言模型（LLM）多智能体系统大多依赖手工设计的工作流，包括预定义的角色（如规划者、求解者、批评者）和通信协议。这种设计方式限制了系统的可扩展性，且缺乏统一的学习框架来优化整个组织架构。
- **研究动机**：随着LLM能力提升，单模型缩放代价日益高昂，而多智能体系统提供了一种替代的缩放维度——通过组织多个模型实例来分解问题、交换中间信息。但现有方法通常将组织视为设计选择，而非可训练的对象。
- **整体含义**：NeuroMAS旨在将多智能体系统转化为可训练的类神经网络架构，通过强化学习联合优化节点间的通信与专业化，使组织本身成为可缩放的能力来源，从而将多智能体设计从“工作流工程”转向“架构设计”。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将多智能体语言系统视为一种前馈的文本值神经网络。LLM智能体作为节点，节点间传递文本信号作为边。节点无角色（role-free）但具有结构感知（structure-aware）——拓扑结构仅决定信息流动路径，具体通信、专业化和协调行为通过联合强化学习从终端奖励中学习。
- **关键技术细节**：
  - **文本值分层架构**：输入经过多个隐藏层，每层包含若干节点。隐藏节点接收上游消息，生成文本并解析为下游消息；输出节点聚合最终层消息生成答案。所有节点共享同一个冻结骨干模型，但各有独立的LoRA适配器作为可训练参数。
  - **角色无关但结构感知**：节点不获得语义角色描述，仅知道其在拓扑中的位置、输入格式和所需输出格式。专业化由训练自然诱导。
  - **联合训练**：整个系统通过REINFORCE算法优化。每个节点的梯度由其自身生成文本的对数概率乘以全局终端奖励（答案是否正确）计算，不依赖中间标签或角色特定奖励。损失函数为各节点代理损失的求和。
  - **渐进式增长**：从小型已训练拓扑扩展为更大的拓扑，继承对应节点的适配器参数，新节点初始化为零，然后继续训练。这避免了从零训练大系统的不稳定性。
- **算法流程（文字说明）**：
  1. 给定输入X和拓扑τ，构造第一层节点的输入上下文（包含X、节点位置）。
  2. 每个第一层节点基于其策略生成文本，解析为下一层每个节点的特定消息。
  3. 逐层重复步骤2，直至最后一层。
  4. 输出节点接收最终层所有消息，生成答案Ŷ。
  5. 计算终端奖励r(Y, Ŷ)，所有节点共享该奖励。
  6. 使用REINFORCE更新每个节点的LoRA参数（基于自身生成文本的对数概率和奖励）。

#### 3. 实验设计：数据集、基准（benchmark）、对比方法
- **数据集与任务**：
  - 推理：ARC-Challenge（科学问答）、BIG-Bench Hard中的Navigate（空间导航推理）。
  - 领域知识：MMLU的三个子集——Abstract Algebra（数学）、College Physics（物理）、Professional Medicine（医学）。
  - 代码生成：HumanEval。
- **基准（benchmark）**：准确率（Accuracy%），经答案规范化后匹配。
- **对比方法**：
  - **固定骨干基线**（不训练模型）：Direct Prompting、Self-Refine、Self-Check、MoA（Mixture-of-Agents）、GoA（Graph of Agents）、AgentNet、GPTSwarm。
  - **训练骨干基线**（更新模型参数）：Single-LLM RL（单模型强化学习）、MALT、CoLLM-CC、以及NeuroMAS的不同拓扑变体（NeuroMAS-3, -5, -7，分别对应3,5,7个LLM调用/样本）。

#### 4. 资源与算力
- **明确信息**：实验在NVIDIA RTX 5090和NVIDIA H100 GPU上进行（附录C.3）。未公布具体GPU数量、训练时长（如epoch数或小时数）。仅说明训练使用AdamW优化器，学习率2e-5，梯度裁剪1.0；每个节点的LoRA适配器参数量可计算（例如NeuroMAS-3总可训练参数6.9M，基于Qwen3-0.6B骨干）。

#### 5. 实验数量与充分性
- **实验数量**：主实验在6个基准上进行（表1）；骨干鲁棒性实验在ARC上用Gemma-3-1B-IT重复（表2）；消融实验分析渐进式增长（表3，Navigate任务）。此外还进行了参数计数控制（表1中Single-LLM RL与NeuroMAS-3对比，参数相同）。总共约6+1+1=8组主要对比（含多指标），加上消融。未进行大规模超参数搜索或多拓扑穷举。
- **充分性评价**：实验覆盖了推理、知识、编码等典型能力，对比了多种现有方法（包括固定和训练方法），并做了骨干鲁棒性检验和路径依赖分析。实验设计较全面，但数据规模有限（每个基准子集大小未明确，可能为中等规模）。未在更大骨干模型（如7B）上验证，也未评估推理延迟和总计算开销。因此，实验充分性良好，但泛化性仍需更多验证。

#### 6. 论文的主要结论与发现
- **主要结论**：
  - NeuroMAS在6个基准上均超过最强的固定骨干基线，并超过参数匹配的单模型RL基线（如ARC上56.5% vs Single-LLM RL的46.0%），表明多智能体组织本身带来能力提升。
  - 骨干鲁棒性：在Gemma-3-1B-IT上NeuroMAS-3达到44.5%，超过所有固定和训练基线。
  - 渐进式增长是扩展的关键：从零训练Large拓扑（NeuroMAS-7）性能下降（40.5% vs 51.0%）；而渐进式增长从NeuroMAS-3逐步扩展到-7，准确率从45.5%提升到51.0%，表明路径依赖性强。
  - 理论分析：当任务具有层次分解结构时，模块化多智能体架构在参数效率上优于非结构化单模型（定理5.1及其推论）。

#### 7. 优点
- **方法创新**：首次将多智能体系统视为可训练的神经网络架构，放弃手工角色，使组织自动优化。
- **联合学习**：所有节点共享终端奖励，无需中间标签，简化了设计。
- **渐进式增长**：揭示了从小到大的训练路径优于直接训练大系统，提供了实用的缩放策略。
- **理论支撑**：给出了参数效率的理论分析，解释了多智能体架构在分解性任务上的优势。
- **实验设计**：参数匹配控制实验（Single-LLM RL vs NeuroMAS-3）有效排除了参数量增加的干扰；骨干鲁棒性检验增强了结论可靠性。

#### 8. 不足与局限
- **实验覆盖有限**：仅使用0.6B和1B小骨干模型，未在更大模型（如7B, 70B）上验证。
- **计算成本高**：每个样本需要多次LLM调用（3-7次），推理延迟增加。未与单模型扩增计算量进行公平比较（例如Single-LLM RL使用多次采样再聚合）。
- **依赖任务分解性**：理论优势仅在任务具有层次分解时成立，可能不适用于全局判断型任务。
- **无超参数优化**：未报告对拓扑、学习率等超参数的详细搜索，可能存在未精细调优的情况。
- **可扩展性边界不明**：渐进式增长仅测试到7个智能体，更大系统（如10+个）的性能未知。
- **基线选择**：部分基线（如GPTSwarm, AgentNet）的参数可能未充分调优，且仅对比了准确率，未对比鲁棒性、收敛速度等。

（完）
