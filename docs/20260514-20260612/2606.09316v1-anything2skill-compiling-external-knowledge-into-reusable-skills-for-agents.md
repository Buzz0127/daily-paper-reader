---
title: "Anything2Skill: Compiling External Knowledge into Reusable Skills for Agents"
title_zh: Anything2Skill：将外部知识编译为智能体可复用技能
authors: "Qianjun Pan, Yutao Yang, Junsong Li, Jie Zhou, Kai Chen, Xin Li, Qin Chen, Liang He"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09316v1"
tags: ["query:agent-build"]
score: 9.0
evidence: 将外部知识编译为智能体可复用技能
tldr: "现有检索增强生成（RAG）主要提供碎片化事实证据，代理需反复推断任务流程，缺乏可复用过程知识。为此提出Anything2Skill框架，将异构知识记录分解并提取为结构化技能契约，构建持久技能库。在qsv和GitHub-CLI任务上分别达98.85%和94.10%成功率，显著优于纯RAG。该方法将隐式流程知识编译为显式技能，实现从知识访问到能力复用的扩展。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09316v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1587, \"height\": 893, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09316v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 778, \"height\": 264, \"label\": \"Table\"}]"
motivation: 现有RAG仅检索碎片化事实证据，代理需重复推断任务流程，缺乏可复用过程知识。
method: 提出Anything2Skill框架，在技能树先验下进行plan-and-expand提取，将异构知识编译为结构化技能契约并管理技能库。
result: "在qsv和GitHub-CLI任务中分别达到98.85%和94.10%成功率，显著优于纯RAG方法。"
conclusion: 通过编译隐式流程知识为显式技能，可有效扩展检索增强代理从知识访问到能力复用的范围。
---

## 摘要
检索增强生成（RAG）使智能体能够在推理时访问外部知识，但主要检索的是碎片化的陈述性证据，导致智能体需要反复从文本、手册、示例、日志或轨迹中推断任务流程。这引发了一个根本性问题：能否将从外部知识库中提取的技能安装到智能体中，使其能快速逼近领域专业知识？本文提出Anything2Skill，一个基于分类法指导的框架，将异构外部知识编译为智能体可复用、可检索、可执行的技能。给定知识记录语料库，Anything2Skill首先将每条记录分解为证据窗口，并在技能树先验下执行“规划与扩展”的技能提取。提取的候选技能随后转化为结构化的技能契约，明确调用条件、禁忌、动作步骤、工作流环节、约束、输出规范、支持证据及置信度分数。为构建可部署的程序性记忆，Anything2Skill通过分类感知编译、注册级协调、生命周期跟踪、版本化更新及可视化技能树投影，将提取的技能持久化管理于SkillBank中。推理时，智能体同时从原始知识库检索任务相关文本段落，并从SkillBank检索相关程序性技能，使得RAG提供陈述性证据，而编译技能提供可复用的程序性指导。在qsv和GitHub-CLI上的实验表明，Anything2Skill结合RAG分别达到98.85%和94.10%的成功率，显著优于纯RAG智能体。这些结果表明，将潜在的程序性知识编译为显式技能，是将检索增强智能体从知识访问扩展到能力复用的有效途径。

## Abstract
Retrieval-augmented generation (RAG) enables agents to access external knowledge at inference time, but it primarily retrieves fragmented declarative evidence, leaving agents to repeatedly infer task procedures from passages, manuals, examples, logs, or trajectories. This raises a fundamental question: can skills extracted from external knowledge bases be installed into an agent, enabling it to rapidly approximate domain expertise? In this paper, we propose Anything2Skill, a taxonomy-guided framework that compiles heterogeneous external knowledge into reusable, retrievable, and executable skills for agents. Given a corpus of knowledge records, \textsc{Anything2Skill} first decomposes each record into evidence windows and performs plan-and-expand skill extraction under a skill-tree prior. The extracted candidates are then converted into structured skill contracts that specify invocation conditions, contraindications, action moves, workflow steps, constraints, output specifications, supporting evidence, and confidence scores. To construct a deployable procedural memory, Anything2Skill manages the extracted skills in a persistent SkillBank through taxonomy-aware compilation, registry-level reconciliation, lifecycle tracking, versioned updates, and visible skill-tree projection. At inference time, agents retrieve both task-specific passages from the original knowledge base and relevant procedural skills from the SkillBank, allowing RAG to provide declarative evidence while compiled skills provide reusable procedural guidance. Experiments on qsv and GitHub-CLI show that Anything2Skill combined with RAG achieves 98.85\% and 94.10\% success rates, respectively, substantially outperforming RAG-only agents. These results suggest that compiling latent procedural knowledge into explicit skills is an effective way to extend retrieval-augmented agents from knowledge access toward capability reuse.

---

## 论文详细总结（自动生成）

# Anything2Skill 论文总结

## 1. 核心问题与整体含义
- **研究动机**：现有检索增强生成（RAG）系统主要提供碎片化的陈述性证据（passages、文档等），智能体在推理时需反复从这些碎片中“推断”任务流程（procedural knowledge），导致效率低下且易出错。尤其在智能体需要调用工具、执行多步操作时，仅依赖事实性检索无法直接提供“如何做”的指导。
- **整体含义**：本文提出应从外部知识库中“编译”出可复用的程序性技能，使智能体能够像拥有领域专业知识一样直接复用流程知识，从而将 RAG 从“知识访问”扩展到“能力复用”。

## 2. 方法论
- **核心思想**：通过分类法（taxonomy）指导的框架，将异构知识记录（文档、手册、对话、日志、轨迹）提取为结构化技能契约，并持久化存储在 SkillBank 中，推理时随原始知识一并检索。
- **关键技术细节**：
  - **技能提取（Skill Extraction with Skill-Tree Prior）**：
    - 定义技能分类树 `T = (A, V, R, π, ℓ, F)`，其中资产类型 `A = {macro_protocol, session_skill, micro_skill, safety_rule, knowledge_reference}`。
    - 将每一条知识记录分解为证据窗口 `wij = ⟨xij, hij, rij, bij, mij⟩`。
    - 采用两阶段“Plan-and-Expand”提取策略：先由规划器（LLM-based planner）从窗口中识别候选技能，然后对每个候选独立扩展为结构化技能草稿。
    - 应用过程有效性过滤器 `Iproc(˜s)`，保留至少包含一个程序性组件（如工作流步骤、动作、约束）且能被映射到分类法的候选。
    - 每个保留的技能草稿附带支持证据记录 `eijk`。
  - **技能管理（Skill Management in SkillBank）**：
    - **文档级编译**：通过分类法感知的身份码 `γ(d)` 将窗口级草稿分组，编译器生成规范技能合约，满足约束（资产类型、粒度、节点不变；仅引用组内证据）。
    - **注册级协调**：对每个入库技能，检索已有相关技能 `Hk(s)`，计算混合匹配分数 `R(s, t)`（结合稠密相似度、BM25、分类兼容性），由管理模型预测生命周期动作（CREATE / STRENGTHEN / REVISE / MERGE / SPLIT / UNCHANGED / DISCARD），并记录版本更新。
    - **可视技能树投影**：将注册库中活跃技能按分类层次组织，选择父节点，进行同级去重（合并重复技能），生成最终 SkillBank `B = ⟨S, E, H, Vhist, L⟩`。
- **推理时使用**：同时从原始知识库检索文档段落，并从 SkillBank 检索相关技能，智能体基于两者回答任务。

## 3. 实验设计
- **数据集/场景**：两个命令行智能体基准：
  - **qsv**：基于官方 qsv 文档，涉及 CSV 命令行操作。
  - **GitHub-CLI**：基于 GitHub CLI 仓库操作指南，覆盖 110 个源文档，编译出 179 个技能（含 1 全局、6 顶层、62 二级、110 微技能）。
- **Benchmark 指标**：任务成功率（%），即智能体能否正确生成可执行命令。
- **对比方法**（四种上下文配置）：
  - **Base Agent**：无额外外部上下文。
  - **RAG**：检索原始知识库 top-3 段落。
  - **ANYTHING2SKILL**：仅从 SkillBank 检索技能，不检索源文档。
  - **ANYTHING2SKILL + RAG**：同时检索技能和源段落。
- **基座模型**：所有配置均使用 `gpt5.4`。

## 4. 资源与算力
- 论文中**未明确说明**使用的GPU型号、数量、训练时长或推理算力消耗。仅提到使用 `gpt5.4` 作为基座模型，但未说明该模型的部署细节或提取/管理阶段的算力需求。

## 5. 实验数量与充分性
- **实验数量**：在 2 个 benchmark（qsv 和 GitHub-CLI）上进行了 4 种配置的对比，共得到 8 个成功率数值（Table 1）。未包含更多消融实验（如不同检索窗口大小、不同分类法设计、不同生命周期动作的影响等）。
- **充分性与公平性**：
  - 实验直接回答了论文提出的两个研究问题，对比设置合理（基座模型统一）。
  - 但仅有两个任务且均为命令行领域，**泛化性验证有限**，没有在其他领域（如机器人、API 调用、多模态）测试。
  - 没有与其他技能提取方法（如 Voyager、DreamCoder 等）进行直接对比，仅与 RAG 和 Base 对比，**对比基线不够全面**。
  - 结果展示清晰，但缺乏误差棒或统计显著性检验。

## 6. 主要结论与发现
- 编译技能可显著提升智能体性能：ANYTHING2SKILL 单独使用即优于 Base Agent（qsv: 81.60%→91.95%，GitHub-CLI: 64.70%→82.30%），说明 SkillBank 提供可靠的过程性指导。
- 技能与 RAG 互补：RAG 单独在 qsv 达 95.41%，但加入技能后提升至 98.85%；GitHub-CLI 上 RAG 仅 76.50%，ANYTHING2SKILL + RAG 达 94.10%，表明过程性知识对复杂工作流尤为重要。
- 整体最佳结果为 ANYTHING2SKILL + RAG，在 qsv 和 GitHub-CLI 上分别达 98.85% 和 94.10%，验证了将隐式流程知识编译为显式技能的有效性。

## 7. 优点
- **方法新颖**：首次系统性地将异构外部知识中的过程性知识编译为结构化技能契约，并建立完整的提取、管理、版本化、投影流程，实现了从陈述性检索到能力复用的跨越。
- **框架完整、可工程化**：包含证据窗口分解、Plan-and-Expand 提取、分类法约束、注册级协调、生命周期管理等模块，设计细致，可落地性强。
- **实验设计清晰**：直接对比了“仅技能”、“仅 RAG”、“两者结合”三种场景，明确阐明了技能和 RAG 的互补关系。
- **结果显著**：在两个 benchmark 上提升幅度大（GitHub-CLI 提升近 30 个百分点），具有较强的实用价值。

## 8. 不足与局限
- **实验覆盖有限**：仅两个命令行任务，未在更广泛的领域（如机器人控制、通用 API 调用、多模态环境）评估，**泛化性存疑**。
- **对比基线不足**：未与其他技能提取或智能体方法（如 Voyager、ReAct with memory、ToolLLM 等）比较，无法确认相对于现有最优方法的增益。
- **缺乏消融实验**：没有分析分类法、Plan-and-Expand 策略、生命周期动作等组件的单独贡献；也未评估不同检索窗口大小或技能管理策略的影响。
- **资源与效率未提及**：未报告技能提取和管理的时间开销、推理时检索延迟，以及大模型（gpt5.4）的调用成本，**实用部署的可行性未验证**。
- **依赖单一基座模型**：所有实验基于 `gpt5.4`，未测试在不同能力水平的 LLM（如开源模型）上的稳定性，存在模型偏差风险。

（完）
