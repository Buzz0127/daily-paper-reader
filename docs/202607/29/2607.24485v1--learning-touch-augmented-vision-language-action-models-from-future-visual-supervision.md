---
title: "τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision"
title_zh: τ：从未来视觉监督中学习触觉增强的视觉-语言-动作模型
authors: "Ning Cheng, Jinan Xu, Wanlin Li, Yangzhi Chen, Jing Gao, Yiqun Wang, Kelan Peng, Wenjuan Han"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24485v1"
tags: ["query:embodied-vla"]
score: 9.0
evidence: 从未来视觉监督学习触觉增强VLA模型
tldr: 触觉信号对机器人精细操作至关重要，但现有VLA模型难以学习有效触觉表示且数据有限。本文提出τ框架，利用未来视觉监督从JEPA中学习动作条件时空触觉表示，与视觉语言特征融合生成动作。引入TacAura数据集，实验证明τ在接触丰富任务中优于基线，并泛化到未见物体和场景，提升操作鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24485v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1811, \"height\": 1004, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1837, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1820, \"height\": 1218, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 829, \"height\": 478, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24485v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1823, \"height\": 402, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24485v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1808, \"height\": 293, \"label\": \"Table\"}]"
motivation: 现有VLA模型缺乏有效触觉表示，且有限演示数据和建模高维触觉信号仍是挑战。
method: 提出τ框架，基于JEPA从未来视觉监督学习动作条件时空触觉表示，与视觉语言特征融合进行动作生成。
result: 在四个接触丰富任务中，τ超越现有模型，并泛化到未见物体和场景。
conclusion: τ证明未来视觉监督可高效学习触觉表示，显著提升VLA模型在有限数据下的操作性能。
---

## 摘要
学习信息丰富的触觉表示并将其有效适配到预训练的视觉-语言-动作（VLA）模型中，在数据和建模层面仍然具有挑战性。在数据层面，有限的任务特定演示限制了表示质量，而大规模预训练则带来高昂成本。在建模层面，现有方法要么关注瞬时接触状态，要么使用六维力/力矩序列对时间交互动态建模，导致高维触觉信号未得到充分探索。为解决这些问题，我们提出τ，一种触觉增强的VLA框架，该框架受联合嵌入预测架构（JEPA）启发，从未来视觉监督中学习动作条件的时空触觉表示，并将其与视觉-语言特征融合，以在数据有限的情况下生成动作。这种监督在潜在空间中运行，仅在训练期间使用，不增加部署开销。我们还引入了TacAura，一个包含同步视觉、本体感觉和基于视觉的触觉信号的数据集，涵盖四个代表性的接触密集型操作任务。实验表明，τ优于现有模型，并能泛化到未见过的物体和场景，提供了改进的操作性能和鲁棒性。

## Abstract
Learning the informative tactile representation while effectively adapting it to pretrained Vision-Language-Action (VLA) models remains challenging at both the data and modeling levels. At the data level, limited task-specific demonstrations constrain representation quality, whereas large-scale pretraining incurs substantial costs. At the modeling level, existing methods either focus on instantaneous contact states or model temporal interaction dynamics using 6D wrench sequences, leaving high-dimensional tactile signals underexplored. To address these challenges, we present τ, a touch-augmented VLA framework that learns an action-conditioned spatiotemporal tactile representation from future visual supervision inspired by the Joint-Embedding Predictive Architecture (JEPA), and fuses it with vision-language features for action generation under limited data. This supervision operates in latent space and is used only during training, adding no deployment overhead. We also introduce TacAura, a dataset of synchronized vision, proprioception, and vision-based tactile signals across four representative contact-rich manipulation tasks. Experiments show that τ outperforms existing models and generalizes to unseen objects and scenes, delivering improved manipulation performance and robustness

---

## 论文详细总结（自动生成）

# τ: 从未来视觉监督中学习触觉增强的视觉-语言-动作模型 — 论文总结

## 1. 核心问题与整体含义

- **研究动机**：通用机器人操作中，Vision-Language-Action (VLA) 模型依靠视觉-语言模态，但在接触密集型任务（如插入、按压、擦除）中缺乏对接触交互动力学（力分布、变形等）的感知。触觉感知能提供这些信息，但现有方法面临两大挑战：
  - **数据层**：有限的特定任务演示难以学习高质量的触觉表示，大规模预训练又成本高昂。
  - **建模层**：现有方法大多关注瞬时接触状态，或仅用低维力/力矩序列建模时间动态，对高维视觉触觉信号的时间演化建模不足。
- **核心含义**：提出 τ 框架，通过未来视觉监督（JEPA 风格）学习动作条件的时空触觉表示，在数据有限的情况下有效融合到预训练 VLA 中，提升接触丰富的操作性能与泛化能力。

## 2. 方法论：核心思想与技术细节

- **整体框架**：基于 π0.5 VLA 骨干，增加触觉编码与适配模块，以及一个辅助的 JEPA 风格预测自监督分支。
- **关键技术细节**：
  - **触觉编码与适配**：使用触觉编码器 `E_tou` 从左右指尖触觉图像提取特征 `z_touch`，再通过可学习触觉适配器 `A_tou` 投影到 VLA 语义空间 `Z_touch`。然后与视觉 tokens、语言 tokens 拼接形成统一表示 `Z = [Z_vision; Z_language; Z_touch]`，送入大语言模型和动作专家生成动作块。
  - **JEPA 风格预测分支（仅训练时使用）**：
    - 当前触觉 tokens `Z_touch` 和动作序列 tokens `Z_action` （通过动作编码器 `E_act` 对向量化 action chunk 编码得到）一起输入轻量 MLP 预测器 `P`，预测未来视觉隐空间表示 `z_vision(t+Δk)`。
    - 目标：从当前和未来观测（通过 VLA 视觉编码器 `E_vis` 提取）计算视觉特征变化 `Δz_vision(t+Δk) = z_vision(t+Δk) - z_vision(t)`，预测变化 `Δ̂z_vision(t+Δk) = ̂z_vision(t+Δk) - z_vision(t)`。
    - 损失函数：**加权余弦对齐损失** `L_SSL`，权重 `w_{t+Δk}` 根据触觉变化幅度裁剪得到，强调接触转换显著的时刻。
  - **联合训练目标**：`L_total = L_IL + λ L_SSL`，其中 `L_IL` 是条件流匹配的模仿学习损失（去噪方向匹配）。λ 平衡两项损失。
- **推理时**：移除预测分支，不增加额外计算量。

## 3. 实验设计

- **数据集**：自建 **TacAura** 数据集，包含四个接触密集型任务：
  - 插头插入 (Plug Insertion)
  - USB 插入 (USB Insertion)
  - 印章按压 (Stamp Press)
  - 白板擦除 (Whiteboard Erasing)
- 每个任务收集 100 条高质量演示（共 400 条），同步多视角 RGB-D、左右指尖视觉触觉、机器人本体感觉与动作，统一采样至 10Hz。
- **对比方法**：
  - 通用 VLA 模型：π0、π0.5
  - 接触操作方法（适应本实验触觉输入）：ForceVLA†、ForceFlow†、T-Rex†（但 T-Rex 未在主要结果表中列出，表1中无 T-Rex，但文中提及）
- **评估指标**：每个任务分三个阶段（抓取/对准/插入 或 拾取/接触/完成），报告各阶段成功率，主要对比“全任务成功率（task-stage final）”。
- **实验设置**：
  - 主实验：20 次试验/任务/模型。
  - 泛化实验：零样本测试USB插入和白板擦除的物体泛化（2个未见物体）和场景泛化（2个干扰设置）。
  - 消融实验：移除动作序列条件、移除JEPA自监督、移除触觉模块，比较全任务成功率。

## 4. 资源与算力

- **训练**：2 块 NVIDIA A800 80GB GPU，batch size 32，训练 30,000 步，采用余弦学习率调度（10,000 warmup，峰值 lr=5e-5）。
- **推理**：单块 NVIDIA RTX 4090 GPU。
- **说明**：文中给出了明确的 GPU 型号、数量、训练步骤，算力信息完整。

## 5. 实验数量与充分性

- **主表实验**：7个模型 × 4任务 × 3阶段，每条件20次试验，共约 7×4×20 = 560 次试验（阶段成功率汇总）。
- **消融实验**：3个变体，在4任务上各20次试验，共 3×4×20 = 240 次试验。
- **泛化实验**：2个任务各 1个已知 + 2个未见物体 + 2个干扰场景，每条件20次试验，共约 2×(1+2+2)×20 = 200 次试验。
- **充分性评估**：
  - 基线选择覆盖了通用VLA和接触专有方法，对比公平（对ForceVLA/ForceFlow适配了相同的视觉触觉特征输入，避免传感器差异）。
  - 消融实验从三个关键组件验证了贡献，顺序合理。
  - 泛化实验涵盖物体级和场景级，并分析了任务依赖性。
  - 但仅四个任务，且每个任务只有100条演示，规模较小；未与大规模预训练的触觉VLA（如π0的触觉版本或Tactile-VLA）进行比较，可能是因为开源限制？总体实验设计合理，结论可信，但覆盖范围有限。

## 6. 主要结论与发现

- τ 的三个变体（腕部监督、前视监督、双视监督）均大幅优于所有基线，平均全任务成功率最高达 71.25%（τ-Wrist Sup.），超过最好基线 ForceVLA† 的 30% 共 41.25 个百分点。
- 性能提升主要来自最终接触执行阶段（插入、按压、擦除完成），初始抓取/拾取成功率各模型均较高。
- 监督视图选择依赖任务：插头/USB插入前视更好（空间对准线索），印章/白板擦除腕部更好（局部接触信息）。双视监督未带来提升，甚至略低，说明多视图融合需改进。
- 泛化实验：白板擦除物体和场景泛化非常稳健（仅下降5个百分点），而USB插入对物体几何和场景干扰敏感，泛化下降明显（40% → 平均30%物体泛化，22.5%场景泛化）。
- 消融：移除触觉模块性能下降最大（-42.5 ppts），移除JEPA自监督次之（-20 ppts），移除动作序列条件最小（-12.5 ppts），验证了各组件重要性。

## 7. 优点

- 方法创新：首次将JEPA风格自监督引入触觉增强VLA，利用未来视觉特征变化学习动作条件的时空触觉表示，不增加推理开销。
- 轻量集成：设计的触觉适配模块可无缝兼容π0/π0.5等VLA骨干，数据流水线最小侵入，便于复现和扩展。
- 数据贡献：公开TacAura数据集（同步触觉+视觉+本体），促进触觉VLA研究。
- 实验扎实：主实验、消融、泛化多层次验证，视野依赖分析具有洞察力。
- 在有限演示（每任务100条）下即能取得显著提升，具有实际部署价值。

## 8. 不足与局限

- **任务与数据规模有限**：仅四个任务，每任务100条演示，可能不足以覆盖复杂接触场景；泛化实验仅针对两个任务，全面性不足。
- **未与更强大的触觉VLA基线比较**：如近期大规模预训练的FTP-1、MLA等，仅对比了适配后的力感知方法，缺少触觉基础模型的对比。
- **双视图融合效果不佳**：双视监督未超越单视，表明多模态融合架构有待改进，文中未深入分析原因。
- **泛化能力局限**：USB插入对几何变化和场景干扰鲁棒性差，说明模型仍依赖视触特征的精细匹配，缺乏对物体类别不变的表示学习。
- **假设猫腻**：方法依赖预训练VLA视觉编码器固定，未探索联合微调触觉和视觉表示，可能限制触觉表示与视觉语义的深层对齐。
- **未考虑长期任务和多阶段场景**：仅单步动作块预测（horizon=32），缺乏长期规划能力。
- **训练成本**：虽然相比大规模预训练便宜，但2×A800训练30k步仍有一定资源要求，文中未报告详细时间。

（完）
