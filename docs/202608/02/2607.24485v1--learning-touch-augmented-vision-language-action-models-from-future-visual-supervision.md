---
title: "τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision"
title_zh: τ：从未来视觉监督中学习触觉增强的视觉-语言-动作模型
authors: "Ning Cheng, Jinan Xu, Wanlin Li, Yangzhi Chen, Jing Gao, Yiqun Wang, Kelan Peng, Wenjuan Han"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24485v1"
tags: ["query:embodied-vla"]
score: 9.0
evidence: 利用未来视觉监督的触觉增强VLA
tldr: 针对触觉表征与预训练VLA模型结合中数据有限和高维信号利用不足的问题，本文提出τ框架，受JEPA启发，利用未来视觉监督在潜空间学习动作条件时空触觉表征，训练时监督、部署无开销，并与视觉语言特征融合生成动作。同时构建TacAura数据集，实验显示τ在接触丰富任务上性能与泛化性优于现有模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24485v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1811, \"height\": 1004}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1837, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1820, \"height\": 1218}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 580}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 829, \"height\": 478}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24485v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1823, \"height\": 402}, {\"url\": \"assets/tables/arxiv/2607.24485v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1808, \"height\": 293}]"
motivation: 现有方法局限于瞬时接触或6D力/力矩，高维触觉信号利用不足，且任务演示数据有限。
method: 提出τ，用未来视觉监督学习动作条件时空触觉表征，与视觉语言特征融合生成动作，监督仅在训练时使用。
result: 在TacAura四个接触丰富任务上，τ超越现有模型，并泛化到未见物体和场景。
conclusion: 未来视觉监督能有效学习触觉表征，提升VLA在接触任务中的性能与鲁棒性。
---

## 摘要
学习信息丰富的触觉表示并将其有效适应于预训练的视觉-语言-动作（VLA）模型，在数据和建模层面仍然具有挑战性。在数据层面，有限的任务特定演示限制了表示质量，而大规模预训练则会带来高昂成本。在建模层面，现有方法要么关注瞬时接触状态，要么使用六维力/力矩序列建模时间交互动态，导致高维触觉信号未被充分探索。为了解决这些挑战，我们提出了τ，一个触觉增强的VLA框架，该框架受联合嵌入预测架构（JEPA）启发，从未来视觉监督中学习动作条件下的时空触觉表示，并将其与视觉-语言特征融合，以在有限数据下生成动作。这种监督在潜在空间中运行，仅在训练期间使用，不会增加部署开销。我们还引入了TacAura，一个包含四个代表性接触丰富操作任务的同步视觉、本体感觉和基于视觉的触觉信号数据集。实验表明，τ优于现有模型，并能泛化到未见过的物体和场景，带来改进的操作性能和鲁棒性。

## Abstract
Learning the informative tactile representation while effectively adapting it to pretrained Vision-Language-Action (VLA) models remains challenging at both the data and modeling levels. At the data level, limited task-specific demonstrations constrain representation quality, whereas large-scale pretraining incurs substantial costs. At the modeling level, existing methods either focus on instantaneous contact states or model temporal interaction dynamics using 6D wrench sequences, leaving high-dimensional tactile signals underexplored. To address these challenges, we present τ, a touch-augmented VLA framework that learns an action-conditioned spatiotemporal tactile representation from future visual supervision inspired by the Joint-Embedding Predictive Architecture (JEPA), and fuses it with vision-language features for action generation under limited data. This supervision operates in latent space and is used only during training, adding no deployment overhead. We also introduce TacAura, a dataset of synchronized vision, proprioception, and vision-based tactile signals across four representative contact-rich manipulation tasks. Experiments show that τ outperforms existing models and generalizes to unseen objects and scenes, delivering improved manipulation performance and robustness

---

## 论文详细总结（自动生成）

好的，以下是对论文《τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision》的详细中文总结。

## 1. 核心问题与整体含义（研究动机与背景）

- **背景**：视觉-语言-动作（VLA）模型通过大规模多模态预训练已成为通用机器人操作的主流范式，但此类模型过度依赖视觉作为主要感知模态，在处理插拔、按压、擦拭等**接触丰富（contact-rich）**任务时存在天然缺陷，因为这些任务的成败高度依赖于接触状态、力分布和形变模式等难以从视觉中直接推断的物理交互信息。
- **核心问题**：论文指出，将触觉信号有效融入预训练VLA模型面临两个层面的挑战：
  - **数据层面**：任务特定的专家演示数据有限，限制了触觉表示的学习质量；而大规模多模态触觉预训练成本极其高昂。
  - **建模层面**：现有触觉增强方法主要关注瞬时接触状态，或将时间动态简化为6D力/力矩序列，对高维、具有细粒度空间接触模式的视觉触觉信号的时序演化建模不足。
- **研究意义**：解决在有限数据和计算预算下，如何学习高维触觉信号的时序交互动态表示，并将其无缝融入预训练VLA模型，以提升接触丰富操作的鲁棒性与泛化能力。

## 2. 方法论：核心思想、关键技术细节与公式

- **核心思想**：提出τ框架，受JEPA（联合嵌入预测架构）启发，通过**预测未来视觉特征变化**作为自监督信号，学习动作条件下的时空触觉表示。关键洞察是：触觉表示应编码"在后续动作作用下物理交互状态如何演化"的动态信息，而非静态接触快照。
- **模型架构**：τ由三大组件构成：
  1. **预训练VLA基座**：采用π0.5作为基础模型，保留其视觉-语言处理与动作生成能力。
  2. **触觉编码与适配模块**：由触觉编码器 $E_{tou}$ 提取原始触觉信号特征，再通过可学习的触觉适配器 $A_{tou}$ 将触觉特征投影到VLA模型的潜空间中，形成与视觉、文本token相兼容的触觉token序列。
  3. **JEPA风格自监督预测分支（仅训练时使用，推理时移除）**：将触觉token与动作序列的编码token拼接，输入轻量级MLP预测器 $P(\cdot)$，预测未来视觉潜特征的变化。
- **训练目标**：联合优化两个目标：
  - **模仿学习目标（动作策略适应）**：采用条件流匹配（conditional flow matching）框架，学习去噪向量场以匹配动作生成的方向：
    $$L_{IL} = \mathbb{E}\left[\| v_\theta(a^s_{t:t+H}, \tilde{o}_t) - u(a^s_{t:t+H}|a_{t:t+H}) \|_2^2\right]$$
  - **预测自监督目标（加权余弦对齐）**：预测未来视觉特征变化而非绝对特征，并根据触觉变化幅度进行加权，目标是：
    $$L_{SSL} = \frac{\sum_{k=1}^{K} w_{t+\Delta_k} \left(1 - \cos(\Delta\hat{z}^{vision}_{t+\Delta_k}, \Delta z^{vision}_{t+\Delta_k})\right)}{\sum_{k=1}^{K} w_{t+\Delta_k} + \delta}$$
    其中权重 $w_{t+\Delta_k} = \text{clip}(\|Z^{(touch)}_{t+\Delta_k} - Z^{(touch)}_t\|_2, w_{min}, w_{max})$，用于强调接触状态显著变化的关键时刻。
  - 总目标为 $L_{total} = L_{IL} + \lambda L_{SSL}$。
- **数据基础设施**：开发了轻量级VTLA数据管线，通过时间戳对齐和多流重采样，将触觉信号作为"即插即用"的附加模态嵌入标准VLA轨迹格式。

## 3. 实验设计：数据集、Benchmark与对比方法

- **数据集**：构建了**TacAura数据集**，包含四项接触丰富任务的同步视觉、本体感觉和视觉触觉信号：
  - 插头插入（Plug Insertion）
  - USB插入（USB Insertion）
  - 印章按压（Stamp Press）
  - 白板擦除（Whiteboard Erasing）
  - 每项任务收集100条高质量演示，共400条轨迹，均带有自然语言标注。
- **硬件设置**：Franka Research 3机械臂，Franka Hand末端执行器，指尖安装DM-Tac WS视觉触觉传感器（320x240，~40FPS），两路静态第三视角RGB-D相机（RealSense D435i）和一路腕部相机（RealSense D405），通过外骨骼遥操作采集人类演示。
- **评估指标**：每项任务划分为三个阶段（如抓取/对齐/插入），报告各阶段成功率与完整任务成功率，共20次试验。
- **对比方法**：
  - 视觉-only VLA基线：π0、π0.5
  - 触觉感知多模态策略：ForceVLA†、ForceFlow†、T-Rex†（†表示针对该触觉传感配置调整但保留原框架）
  - τ的三个变体：使用腕部视图监督（τ-Wrist Sup.）、前视图监督（τ-Front Sup.）、双视图监督（τ-DualView Sup.）作为自监督目标信号。

## 4. 资源与算力

- **训练硬件**：2块NVIDIA A800 80GB GPU，训练30,000步，batch size为32，峰值学习率5e-5，10,000步warmup，余弦学习率调度。
- **推理硬件**：单块NVIDIA RTX 4090 GPU。
- **训练时长**：论文未明确说明具体训练时间（小时数），仅给出训练步数与GPU配置。

## 5. 实验数量与充分性评估

论文进行的三类实验总体上较为充分：

- **主实验**：在4个任务上对7个模型进行了20次试验的评估，覆盖了视觉基线和触觉增强的基线。
- **泛化实验**：对USB插入和白板擦除两项任务进行了零样本泛化测试，包括两个未见物体和两个干扰物场景配置，共4种泛化条件。
- **消融实验**：进行了三组消融，分别移除动作序列条件、预测自监督目标和整个触觉编码与适配模块。

**充分性与公平性分析**：
- **积极方面**：将力传感器方法适配到视觉触觉传感器，以公平比较触觉信号形式差异，做法值得肯定。分阶段成功率提供了细致的性能分析，有助于定位基线模型失败的具体环节。消融设计清晰，能有效隔离各组件贡献。
- **不足之处**：每项任务仅提供100条演示，规模较小。基线中T-Rex未被纳入主实验表格。消融实验的"去除影响"不能简单加和，论文虽已说明这一点，但未分析组件间交互效应。泛化实验仅聚焦于两项任务而非全部。实验未提供方差或置信区间信息，在20次试验下，约5个百分点的差异可能不具有统计显著性。论文未提及随机种子设置，可能影响可复现性。

## 6. 主要结论与发现

- **核心结论**：τ框架在四项接触丰富任务上的完整任务成功率上大幅超越所有基线。最佳变体τ-Wrist Sup.平均成功率达71.25%，超过最强基线ForceVLA†（30.00%）41.25个百分点。
- **分任务表现**：在插头插入、USB插入、印章按压和白板擦除上分别超越最强基线45、30、20和45个百分点。
- **监督视角的任务依赖性**：前视图监督更适合对齐和插入任务（提供空间定位线索）；腕部视图监督更适合接触和擦拭任务（提供局部接触信息）；双视图监督反而表现次优，表明多视角融合需要更有效的机制。
- **泛化能力**：白板擦除在物体和场景泛化上表现稳健；USB插入对物体几何变化和场景干扰较为敏感，泛化性能下降明显。
- **消融结果**：触觉编码与适配模块贡献最大（移除后下降42.5个百分点），预测自监督学习其次（下降20个百分点），动作序列条件再次（下降12.5个百分点）。三个组件均对最终执行阶段（而非初始抓取阶段）贡献显著。

## 7. 优点

- **方法论创新**：将JEPA的未来预测思想引入触觉-VLA学习，通过在潜空间预测未来视觉特征变化来监督触觉表示，避免了像素级重建的高成本，且仅在训练时使用，**零推理开销**。
- **动作条件化设计**：触觉表示学习受动作序列条件约束，使表示与决策过程直接关联，更符合操控任务本质。
- **模块化架构**：触觉适配器将触觉token投影到VLA语义空间，保持预训练模型能力不变，可适配不同触觉传感器。
- **轻量化数据管线**：以"最小侵入"原则将触觉模态嵌入标准VLA轨迹格式，有助于社区扩展。
- **实验设计细致**：分阶段评估准确揭示了基线模型在最终完成阶段而非初始交互阶段的不足，提供了更深入的分析视角。

## 8. 不足与局限

- **数据规模有限**：每项任务仅100条演示，虽意在验证小样本下的有效性，但限制了训练的稳定性和泛化结论的普适性。
- **任务范围偏窄**：仅涉及4类桌面级操作任务，未覆盖更复杂的长期、多阶段操作任务。
- **统计严谨性不足**：论文未报告多次运行的标准差或置信区间，也未提及是否使用多个随机种子，20次试验下的性能差异可能受随机性影响。
- **泛化评估不全面**：零样本泛化实验仅局限于USB插入和白板擦除，且对USB插入的性能下降缺乏深入的原因剖析（如几何变化与场景干扰的交互效应）。
- **消融分析不完整**：自监督预测目标与触觉编码器可能存在强交互，但论文未分析不同变体下消融的异同；加性假设被说明为不成立，但未探索替代分析框架。
- **双视图变体表现意外**：双视图组合反而低于最优单视图，论文仅指出"需要更有效的视图融合机制"，未深入分析失败原因。
- **基线适配可能引入偏差**：对ForceVLA等基线替换触觉传感器输入虽出于公平性考虑，但可能干扰这些框架原始设计的假设，导致其结果偏保守。

（完）
